<Page x:Class="YetAnotherMonitor.Views.Pages.ConversionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d:DataContext="{d:DesignInstance local:ConversionPage}"
      xmlns:local="clr-namespace:YetAnotherMonitor.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ConversionPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0" Text="{Binding InputValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="AcceptsReturn" Value="True"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="WhiteSmoke"/>
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                </Style>
            </TextBox.Style>
        </TextBox>

        <StackPanel Grid.Column="1" Orientation="Vertical">
            <Button Style="{StaticResource DefaultButton}" Content="Encode »" Height="32" Width="120" Margin="0,0,0,8" Command="{Binding EncodeCommand}" />
            <Button Style="{StaticResource DefaultButton}" Content="Decode »" Height="32" Width="120" Margin="0,0,0,8" Command="{Binding DecodeCommand}"/>
            <Button Style="{StaticResource DefaultButton}" Content="«- Swap -»" Height="32" Width="120" Margin="0,0,0,8" Command="{Binding SwapCommand}"/>
            <Button Style="{StaticResource DefaultButton}" Content="Copy Result" Height="32" Width="120" Command="{Binding CopyToClipboardCommand}"/>
        </StackPanel>

        <TextBox Grid.Column="2" Text="{Binding OutputValue, Mode=TwoWay}">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="AcceptsReturn" Value="True"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="WhiteSmoke"/>
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                </Style>
            </TextBox.Style>
        </TextBox>
    </Grid>
</Page>
